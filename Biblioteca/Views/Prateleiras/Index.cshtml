@model IEnumerable<DataBiblioteca.Prateleira>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Localizacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Localizacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrateleiraID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrateleiraID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrateleiraID })
        </td>
    </tr>
}

</table>
